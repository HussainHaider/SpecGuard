# SpecGuard

<!-- One paragraph: what goes in, what comes out, who would use it. No stack list here. -->

![demo](docs/demo.gif)
<!-- Above the fold. A 15s GIF of upload -> verdict -> evidence panel. Record this at M6. -->

## What this is and isn't
<!-- The disclaimer, high on the page, not in a footer:
     independent portfolio project, not affiliated with any retailer,
     public EUR-Lex text + synthetic specs authored in this repo, no proprietary data. -->

## Quickstart
<!-- `docker compose up` + one seed command. If it takes more than two commands, fix the
     code, not the docs. Verified from a clean clone at M7. -->

## Architecture
<!-- Diagram + one paragraph walking a single request from upload to verdict.
     Name the boundary: which checks are deterministic Python, which use the model. -->

## Design decisions
<!-- 5-8 entries, 5 lines each: context / options / choice / what it costs.
     Pulled from docs/decisions.md. At minimum: why some rules never touch an LLM;
     why vectors live in Qdrant and not Postgres; why chunk_id is deterministic;
     why abstention over best-guess; why no reranker; why not serverless. -->

## Evaluation
<!-- Golden set: size, four sources, how labels were produced, dev/held-out split.
     Metrics table, baseline vs current, both splits shown separately.
     Tier 1 (deterministic, gates CI) vs tier 2 (judged, reported only) and why.
     Then a short honest paragraph on where the numbers are weak. -->

## Guardrails
<!-- Table: threat -> mitigation -> what the system does when it fires.
     Rows: injected instructions in supplier PDFs, unresolvable citation,
     low extraction confidence, allergen FAIL, oversized/wrong-type upload. -->

## Operations
<!-- The section most portfolio projects don't have.
     Metrics exposed at /metrics. Three alerts with thresholds and why those numbers.
     Runbook: "abstention rate spiked", "eval gate failing after a prompt change".
     Measured cost per spec and p50/p95 latency. -->

## Deployment
<!-- Single VM behind Caddy, compose file is the deployment.
     Provider and tracing behind interfaces. Why not serverless. -->

## Data sources and licensing
<!-- EUR-Lex documents used and when downloaded. Synthetic specs generated by
     fixtures/specs/generate.py. n8n community edition under the Sustainable Use License.
     Model providers used. -->

## What I'd build next
<!-- Ranked, with reasons. Reranking, expert relabelling of the golden set,
     more regulations, agent trajectory metrics. -->

## What I deliberately didn't build
<!-- Auth, multi-tenancy, multi-agent orchestration, fine-tuning, ERP integration.
     Say why each was out of scope for a two-week build. This section is a feature. -->
